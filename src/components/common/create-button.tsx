import CommonButton from "./common-button";

const CreateButton = () => {
  return (
    <CommonButton action="add" href="add">
      Tạo mới
    </CommonButton>
  );
};

export default CreateButton;
